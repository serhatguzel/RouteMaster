version: '3.8'
services:
  routemaster-api:
    build: .
    container_name: routemaster_app
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=default
    restart: unless-stopped